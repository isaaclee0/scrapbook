<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scrapbook.io</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            text-align: center;
            background-color: #f8f9fa;
        }

        h1 {
            margin-top: 20px;
            color: #333;
        }

        .gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 15px;
            padding: 20px;
            max-width: 1200px;
            margin: auto;
        }

        .pin {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: transform 0.2s ease-in-out;
        }

        .pin:hover {
            transform: scale(1.05);
        }

        .pin img {
            width: 100%;
            height: auto;
            max-height: 300px; /* Prevents very tall images from stretching the layout */
            display: block;
            object-fit: cover; /* Ensures uniform image display */
        }

        .pin p {
            padding: 10px;
            font-size: 14px;
            color: #555;
        }
    </style>
</head>
<body>

    <h1>Scrapbook.io</h1>
    <p>{{ status }}</p>

    <div class="gallery">
        {% for pin in pins %}
        <div class="pin">
            <a href="{{ pin['link'] }}" target="_blank">
                <img src="{{ pin['media'][0]['url'] }}" loading="lazy" alt="{{ pin['title'] }}">
            </a>
            <p>{{ pin['description'] }}</p>
        </div>
        {% endfor %}
    </div>

</body>
</html>
